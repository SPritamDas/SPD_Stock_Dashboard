streamlit>=1.42        # 1.42+ for st.column_config + dataframe row-selection used throughout the app
plotly
yfinance>=0.2.66       # the data spine — check before upgrading; yfinance breaks its API often
gspread
gspread-dataframe
google-auth
pandas
numpy
requests
beautifulsoup4
lxml
openpyxl
# NOTE: Authlib + httpx used to be here for Streamlit's native OIDC st.login(). This app no longer
# uses it — auth.py is a shared-password gate (st.login looped on Streamlit Community Cloud with
# "Missing provider for OAuth callback"). Removed to keep the Cloud install lean.
